rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Chapters — public read, authenticated write (for seed)
    match /materi/{chapterId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // User docs — only owner can read/write
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
  }
}
